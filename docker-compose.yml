services:
  db:
    image: postgres:15
    container_name: soloquest-db
    environment:
      POSTGRES_DB: soloquest
      POSTGRES_USER: postgres
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
